[project]
name = "my-python-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AAA-huan", email = "2767151990@qq.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "python-dotenv>=1.2.2",
]

[project.scripts]
my-python-lab = "my_python_lab:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
